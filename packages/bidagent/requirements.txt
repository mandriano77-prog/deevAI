# Core
requests>=2.31
pyyaml>=6.0
pandas>=2.1                # for Antenna result handling (next module)

# Antenna SQL access (connector depends on Beeswax setup; typically Postgres-compatible)
psycopg2-binary>=2.9       # swap for snowflake-connector or pyathena if needed

# Digest (LLM) — pick one based on digest.llm_provider in config
anthropic>=0.30
# openai>=1.0              # uncomment if you want OpenAI as alt provider

# Delivery
boto3>=1.34                # S3 + Secrets Manager (used by Lambda + delivery)

# Dev / test only (move to requirements-dev.txt later)
pytest>=8.0
